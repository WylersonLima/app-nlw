// arrays, objetos
let metas = ["wylerson", "aluno"]

console.log(metas[1] + ": " + metas[0])

//objeto

let meta = {
    value: 'ler um livro por mês',
    checked: false            
}

console.log(meta.value)

//function

const criarMeta = (meta) => {

}